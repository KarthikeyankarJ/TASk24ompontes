import React from 'react'
import Root from '../base/Root'

function Company() {
  return (
    <Root
    description={"Navigate through Components to See the React Routing work"}
    >
    </Root>
  )
}

export default Company